# dependencies
node_modules/

# local assistant config and notes (not part of the project)
CLAUDE.md
.claude/

# secrets: the relay key lives in the deployment, never in the repo
.env
.env.local
relay/.env
relay/.env.local

# generated by the visual test on failure
design/baselines/*.diff.png

# eval output and scratch
eval/results/
*.log

# OS and editor files
.DS_Store
Thumbs.db
.vscode/
.idea/
